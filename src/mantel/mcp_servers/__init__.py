"""Built-in MCP servers shipped with mantel (launched as stdio subprocesses).

Each module here is runnable as ``python -m mantel.mcp_servers.<name>`` and speaks
MCP over stdio, so it can be wired into mantel's ``mcp_servers`` config like any
external server.
"""
